'use strict'

addEventListener("load", () => {

    try{                                    //se inicia el try
        var year = 2019                     //declaracion de varaible
        alert (yea)                         //Se escribe mal la variable year

        console.log(decodeURIComponent("www.google.com"))      //Imprime en consola el url comprobado. decodeURI revisa que exista el URL

    }catch(error){                         //Si hay un error en try, cacha el error
        console.log(error)                 //Imprime en consola el error
    }

})